# =============================================================================
# COINCALL API — copy to server/.env (NEVER commit real secrets)
# =============================================================================
#
# SETUP CHECKLIST:
# 1. Agora: https://console.agora.io → App ID + Primary Certificate
# 2. EasyPaisa / JazzCash merchant credentials from your bank dashboard
# 3. Google Play: service account JSON for purchase token verification
# 4. Apple: App Store Connect shared secret / App Store Server API keys
# 5. Optional: MONGODB_URI (or mongodb DATABASE_URL) for durable multi-instance snapshots
#    Disk (.data/) remains the always-on fallback if Mongo is unset/unreachable.
#
# =============================================================================

PORT=4000
ADMIN_API_KEY=change-me-admin-key

AGORA_APP_ID=
AGORA_APP_CERTIFICATE=

# Public CDN for AI fallback clips
AI_HOST_CDN=

# Public absolute API URL for media library asset links (optional)
# PUBLIC_API_URL=https://your-api.example.com

# Withdrawal gateways (host cash-out)
EASYPAY_MERCHANT_ID=
EASYPAY_STORE_ID=
EASYPAY_HASH_KEY=
JAZZCASH_MERCHANT_ID=
JAZZCASH_PASSWORD=
JAZZCASH_INTEGRITY_SALT=
BANK_PAYOUT_WEBHOOK_SECRET=

# IAP verification
GOOGLE_PLAY_PACKAGE_NAME=com.yourcompany.luma
GOOGLE_PLAY_SERVICE_ACCOUNT_EMAIL=
# Path or raw JSON for Google Play Developer API
GOOGLE_PLAY_SERVICE_ACCOUNT_JSON=
APPLE_IAP_SHARED_SECRET=
APPLE_IAP_BUNDLE_ID=com.yourcompany.luma

# Optional persistence
# DATA_DIR=/var/data          # Render persistent disk mount
# ALLOW_IAP_STUB=1            # allow unverified IAP credits (demo only)
# ALLOW_FREE_VIP=1            # allow unpaid VIP toggle (demo only; blocked in production by default)
# MONGODB_URI=mongodb+srv://...   # preferred for multi-instance wallets
# MONGODB_DB=coincall
DATABASE_URL=
REDIS_URL=
